name: Unity - Test runner
  uses: game-ci/unity-test-runner@v2.1.0
  env:
    UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
  with:
    projectPath: elnzburn/fuckyo
    githubToken: ${{ secrets.GITHUB_TOKEN }}
    unityVersion: auto
    testMode: All